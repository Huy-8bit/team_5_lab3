#include "function.h"
#include "function.cpp"
int main(int argc, char** argv) {
    system("cls");
    system("color 0a");
    srand((int)time(NULL));
    vector<int> arr;
    control_functions(arr);
    return 0;
}

